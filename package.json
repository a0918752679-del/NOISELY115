{
  "name": "ntpc-noise-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "New Taipei mobile sound camera dashboard with admin permissions and Zeabur deployment support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node --check server.js",
    "verify": "node --check server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "xlsx": "^0.18.5",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
